// Enter the IP address of the mock server here.
// It works with localhost when using an iPhone simulator,
// but for android simulators or even real devices, you should enter the ip address
// which you might find e.g. in your router or via `ifconfig`.
export const API_BASE_URL = "http://127.0.0.1:3004";
